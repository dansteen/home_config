# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/"6: docs"/userguide.html for a complete reference!

set $mod Mod4
set $mod4 Mod4
set $alt Mod1
set $1 "1:term"
set $2 "2:web"
set $3 "3:apps"
set $4 "4:mail"
set $5 "5:5"
set $6 "6:jenkins"
set $7 "7:mirage"
set $8 "8:sequeltracker"
set $9 "9:personal"

# font!  for window titles. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
#font pango:MiscFixedSC13 9
font pango:Droid Sans Mono 9

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# set our default workspace orientation
workspace_layout tabbed

# we want our focus to wrap rather than moving to the next container
force_focus_wrapping yes

# we want to be able to return to our previous workspace
workspace_auto_back_and_forth no

# start a terminal
#bindsym $mod+Return exec st -f "Misc Fixed:pixelsize=13:style=SemiCondensed" -e /bin/zsh #tmux -L outer -f ~/.tmux_outer.conf
# the below is with a ttf version of Misc Fixed installed
bindsym $mod+Return exec st -f "MiscFixedSC613:pixelsize=24" -e /bin/zsh #tmux -L outer -f ~/.tmux_outer.conf
bindsym $mod+Shift+Return exec tmux -L inner -f ~/.tmux_inner.conf new-window

# kill focused window
bindsym $mod+Shift+C kill

# start a launcher with some options
bindsym $mod+r exec rofi -show run
bindsym $mod+p exec rofi -modi lpass:/home/dsteen/scripts/rofi-lpass -show lpass
bindsym $mod+g exec rofi -show window
bindsym $mod+y exec rofi -modi clipboard:greenclip\ print -show clipboard


# enable debug log
bindsym $mod+x debuglog toggle

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+H move left
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right

# split in horizontal orientation
#bindsym $mod+n split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
#bindsym $mod+t layout stacking
#bindsym $mod+w layout tabbed
#bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+minus floating toggle

# change focus between tiling / floating windows
bindsym $mod+minus focus mode_toggle

# focus the parent container
bindsym $mod+u focus parent

# focus the child container
bindsym $mod+d focus child

#bindsym $mod+space [window_role="chat"] scratchpad show
bindsym $mod+Shift+equal move scratchpad
bindsym $mod+equal scratchpad show

# setup our mark for the scratchpad
bindsym $mod+Shift+m mark scratchpad
# we can switch this back to $mod when we are not on the mac
bindsym $alt+Tab [con_mark="scratchpad"] scratchpad show

# workspaces
#workspace "1: term" output 0
#workspace "2: web" output 1
#workspace "3: mail" output 1
#workspace "4: work" output 1
#workspace "5: dev" output 1
#workspace "6: docs" output 1
#workspace "8: im" output 1
#workspace "9: temp" output 1


# switch to workspace
bindsym $mod+1 workspace $1
bindsym $mod+2 workspace $2
bindsym $mod+3 workspace $3
bindsym $mod+4 workspace $4
bindsym $mod+5 workspace $5
bindsym $mod+6 workspace $6
bindsym $mod+7 workspace $7
bindsym $mod+8 workspace $8
bindsym $mod+9 workspace $9

# next/prev workspace
#bindsym $mod1+k workspace next
#bindsym $mod1+j workspace prev

# move focused container to workspace
bindsym $mod+Shift+1 move workspace $1
bindsym $mod+Shift+2 move workspace $2
bindsym $mod+Shift+3 move workspace $3
bindsym $mod+Shift+4 move workspace $4
bindsym $mod+Shift+5 move workspace $5
bindsym $mod+Shift+6 move workspace $6
bindsym $mod+Shift+7 move workspace $7
bindsym $mod+Shift+8 move workspace $8
bindsym $mod+Shift+9 move workspace $9

# client binding
assign [class="URxvt"] $1
#assign [class="Firefox"] $2
#assign [class="uzbl-core"] 2: web
#assign [class="Pidgin"] 8: im
assign [class="Thunderbird"] 4: mail
assign [class="Evolution"] 4: mail
assign [class="Wfica"] 7: citrix
assign [class="Emacs"] 3: dev
assign [title=".* - VIM$"] 3: dev
assign [class="libreoffice-writer"] 6: docs
assign [class="libreoffice-calc"] 6: docs
assign [class="VCLSalFrame.DocumentWindow"] 6: docs
for_window [class="XTerm"] floating enable
for_window [class="XTerm"] border 1pixel
for_window [con_mark="scratchpad"] move scratchpad
for_window [title="Add to Chat"] floating enable
for_window [class="Adl"] move scratchpad 
for_window [class="Hipchat"] move scratchpad 
for_window [class="Adl" title="^$"] floating enable, border none

# nemo 
bindsym $mod+b exec raiseorlaunch -e nemo -i -c nemo -r
# WorkFlowy
bindsym $mod+n exec raiseorlaunch -e "chromium --app=https://workflowy.com" -i -s "workflowy.com" -r
# slack
bindsym $mod+s exec raiseorlaunch  -e "chromium --app=https://traitify.slack.com" -i -s "traitify.slack.com" -r 
bindsym $mod+z exec raiseorlaunch  -e "chromium --app=https://mirageid.slack.com" -i -s "mirageid.slack.com" -r 
bindsym $mod+Shift+z exec raiseorlaunch  -e "chromium --app=https://embarkstudio.slack.com" -i -s "embarkstudio.slack.com" -r 
#bindsym $mod+Ctrls exec raiseorlaunch  -e "chromium --app=https://sequeltracker.slack.com" -i -s "sequeltracker.slack.com" -r 
# pulse
bindsym $mod+c exec raiseorlaunch  -e "chromium --app=https://messenger.klinkerapps.com" -i -s "messenger.klinkerapps.com" -r
# whatsapp
bindsym $mod+w exec raiseorlaunch -e "chromium --app=https://web.whatsapp.com" -i -s "web.whatsapp.com" -r
# asana
bindsym $mod+t exec raiseorlaunch -e "chromium --app=https://app.asana.com" -i -s "app.asana.com" -w 3:apps
# jenkins
bindsym $mod+e exec raiseorlaunch -e "chromium --app=https://jenkins.traitify.com" -i -s "jenkins.traitify.com" jenkins
# mail
bindsym $mod+i exec --no-startup-id raiseorlaunch -e "chromium --app=https://inbox.google.com/u/1" -i -s "inbox.google.com__u_1" -w mail
bindsym $mod+Shift+n exec --no-startup-id raiseorlaunch -e "chromium --app=https://inbox.google.com/u/0" -i -s "inbox.google.com__u_0" -w mail
bindsym $mod+Shift+i exec --no-startup-id raiseorlaunch -e "chromium --app=https://inbox.google.com/u/2" -i -s "inbox.google.com__u_2" -w mail
# AWS
bindsym $mod+a exec --no-startup-id raiseorlaunch -e "chromium --app=https://woofound.signin.aws.amazon.com" -i -s "woofound.signin.aws.amazon.com" -w aws

# reload the configuration file
bindsym $mod+Shift+R reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+Q restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

# resize "4: win"dow (you can also use the mouse for that)
#mode "resize" {
 #       # These bindings trigger as soon as you enter the resize mode
#
        # They resize the border in the direction you pressed, e.g.
        # when pressing left, the "4: win"dow is resized so that it has
        # more space on its left

#bindsym j resize shrink left 10 px or 10 ppt
#bindsym Shift+J resize grow   left 10 px or 10 ppt

#bindsym k resize shrink down 10 px or 10 ppt
#bindsym Shift+K resize grow   down 10 px or 10 ppt

#bindsym l resize shrink up 10 px or 10 ppt
#bindsym Shift+L resize grow   up 10 px or 10 ppt

#bindsym semicolon resize shrink right 10 px or 10 ppt
#bindsym Shift+colon resize grow   right 10 px or 10 ppt

        # same bindings, but for the arrow keys
#bindsym Left resize shrink left 10 px or 10 ppt
#bindsym Shift+Left resize grow   left 10 px or 10 ppt

#bindsym Down resize shrink down 10 px or 10 ppt
#bindsym Shift+Down resize grow   down 10 px or 10 ppt

#bindsym Up resize shrink up 10 px or 10 ppt
#bindsym Shift+Up resize grow   up 10 px or 10 ppt

#bindsym Right resize shrink right 10 px or 10 ppt
#bindsym Shift+Right resize grow   right 10 px or 10 ppt

        # back to normal: Enter or Escape
#bindsym Return mode "default"
#bindsym Escape mode "default"
#}

#bindsym $mod+r mode "resize"

bar {
  status_command i3status
#  mode hide
#  hidden_state hide
#  modifier $mod
#  tray_output primary

}
# set up jumping to a specific window
bindsym $mod+slash exec i3-input -F '[title="%s"] focus' -P 'find: ' 

# hot key for xcreensaver
#bindsym $mod+F11 exec /usr/bin/xscreensaver-command -lock
bindsym $mod+F11 exec /usr/bin/i3lock --dpms

# our ssh launcher
#bindsym $mod+s exec i3-input -p 'exec ~/scripts/i3_ssh.sh '  -P 'ssh: '
# ssh to boxes based on role
bindsym $mod+o exec i3-input -p 'exec ~/scripts/open_servers.sh ' -P 'role: '

# our machine setup
bindsym $mod+Up exec i3-input -p 'exec ~/projects/scripts/machine_setup.sh -s '  -P 'push to: '

# rename a workspace
#bindsym $mod1+r exec i3-input -F "exec ~/scripts/i3_rename_workspace.sh '%s'"  -P 'Workspace Name: '


# add crf
#bindsym $mod+backslash exec i3-input -F "exec ~/projects/scripts/generate_ticket.sh -k SD -t Task -x -u dsteen -p 'Bel7?mid' -s '%s'"  -P 'Add SD ticket: '

#  add sd task
#bindsym $mod+Shift+backslash exec i3-input -F "exec ~/projects/scripts/generate_ticket.sh -k CRF -t CRF -x -u dsteen -p 'Bel7?mid' -r jpollara -d '%s' -s '%s'"  -P 'Create CRF: '


