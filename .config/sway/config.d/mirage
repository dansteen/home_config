# This config is for our personal workspace setup
# switch to workspace
bindsym $mod+m workspace mirage; mode "mirage"
# move window to workspace
bindsym $mod+Shift+m move container to workspace mirage

mode "mirage" {
}
